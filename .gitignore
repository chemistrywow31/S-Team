# --- dependencies / OS noise ---
node_modules/
.DS_Store

# --- personal local config (per-machine, not shared) ---
.claude/settings.local.json

# --- runtime products (generated by every team run; never committed) ---
/.worklog/
/output/

# --- gate run artifacts (regenerated on every run, any depth) ---
render-gate-report.json
layout-gate-report.json
render-gate-screenshots/
